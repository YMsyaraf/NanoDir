Commands
-change color
color

-exit
exit
or press escape

Navigate and Select Files:
Use the arrow keys (up and down) to navigate through the list of files.
Press the space bar to select or deselect files. Selected files will be highlighted in green.
Execute Commands:
To delete the selected files, type the command delete and press Enter.
To copy the selected files to the current directory, type the command copy and press Enter.
Example Workflow:
Start your application.
Navigate to the files you want to select using the arrow keys.
Press the space bar to select multiple files.
Type delete and press Enter to delete the selected files.
Or type copy and press Enter to copy the selected files to the current directory.

-other command same like system one
